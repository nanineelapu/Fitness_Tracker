apiVersion: v1
kind: ConfigMap
metadata:
  name: fitness-configmap
data:
  # Non-sensitive runtime configuration only.
  # The connection string lives in the Secret (fitness-secret).
  PORT: "5000"
  HOST: "0.0.0.0"
  NODE_ENV: "production"
